//
// Procedurally generated namecard patterns
// Author: Xavier Ho <contact@xavierho.com>
//

var canvas;
const width = 1062,
      height = 652;

window.setup = () => {
  canvas = createCanvas(width * 0.5, height * 0.5);
  canvas.parent('space');
};

window.draw = () => {
  background(128, 128, 128);
};